 
 <%= javascript_include_tag "game/game" %>
<script>
 
</script>  
game <%=session[:current_game]%>

<div class="result" id="result">
  <h1> played words</h1>
  <table>
    <tr>
     <th>Player</th>
     <th>Word</th>
     <th>score</th>         
    </tr>
<%if(@game.playedWords)%>
<%@game.playedWords.size.times do |i| %>      
    <tr>
    <td><%=@game.playedWords[i].player.name%></td>
    <td><%=@game.playedWords[i].word%></td>
     <td><%=@game.playedWords[i].word.length%></td>
    </tr>
  <%end%>
    <%end%> 
  </table>
</div>

<table id="grid">
  <%  @game.size.times do |i| %>
    <tr>

      <%  @game.size.times do |j| %>
      <td  id='target<%=  "#{i}.#{j}"%>' onclick="word(<%=i%>,<%=j%>)"><%=  @game.grid[i][j] %></td>
      <%end%>
    </tr>
  <%end%>
</table>

<div id="dialog" >

<form>
<fieldset>
<label for="letter">Letter</label>
<input type="text" name="letter" id="letter" class="text ui-widget-content ui-corner-all" length="1" maxlength="1" />
<label for="letter">Word</label>
<input type="text" name="word" id="word" class="text ui-widget-content ui-corner-all" />
</fieldset>
</form>
</div>

